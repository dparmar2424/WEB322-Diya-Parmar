<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Find Your Lover - Member Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../css/style.css">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center display-4 mb-4">Discover More About Your Potential Match, <%= user.firstName %>...</h1>
        <div class="row">
            <div class="col-md-6">
                <div class="mb-3">
                    <h5>Member ID:</h5>
                    <p><%= user.id %></p>
                </div>
                <div class="mb-3">
                    <h5>Known Alias:</h5>
                    <p><%= user.lastName %></p>
                </div>
                <div class="mb-3">
                    <h5>Contact:</h5>
                    <p><%= user.email %></p>
                </div>
            </div>
            <div class="col-md-6">
                <div class="mb-3">
                    <h5>Date of Birth:</h5>
                    <p><%= user.dob.split('T')[0] %></p>
                </div>
                <div class="mb-3">
                    <h5>Affiliation:</h5>
                    <p><%= user.company %></p>
                </div>
                <div class="mb-3">
                    <h5>Emergency Contact:</h5>
                    <p><%= user.phone %></p>
                </div>
            </div>
        </div>
        <div class="text-center mt-5">
            <button class="btn btn-primary" onclick="history.back()">Return</button>
        </div>
    </div>
</body>
</html>
